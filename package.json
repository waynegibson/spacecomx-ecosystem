{
  "name": "@spacecomx/ecosystem",
  "private": true,
  "packageManager": "pnpm@8.7.4",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "clean": "pnpm -r clean",
    "clean:all": "pnpm -r clean:all && rm pnpm-lock.yaml && rm -rf node_modules"
  },
  "devDependencies": {
    "@spacecomx/eslint-config-antfu": "workspace:*",
    "@types/node": "^20.6.2",
    "typescript": "^5.2.2"
  }
}
